import { Box } from "@mui/material";
import styled from "styled-components";

export const StyledBox = styled(Box)({
  maxWidth: "400px",
  margin: "auto",
  marginTop: "200px",
  marginBotton: "50px",
});
